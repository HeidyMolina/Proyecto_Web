<!-- views/forgot-password.ejs -->

<% if (typeof msg !== "undefined" && msg) { %>
  <script>
    document.addEventListener("DOMContentLoaded", () => {
      Swal.fire({
        title: '🔐 <b><%= msg.title || "Recuperar Contraseña" %></b>',
        text: '<%= msg.text || "Revisa tu correo para continuar con la recuperación." %>',
        icon: '<%= msg.type || "info" %>', // success | error | warning | info
        background: 'linear-gradient(135deg, #5a00c6, #2a6cf6)',
        color: '#fff',
        confirmButtonText: '✅ Entendido',
        showCancelButton: false,
        customClass: {
          popup: 'animate__animated animate__zoomIn',
          title: 'fw-bold fs-4',
          confirmButton: 'btn btn-success px-4 py-2 fw-bold',
        },
        buttonsStyling: false
      });
    });
  </script>
<% } %>

<style>
  body {
    background: url('/images/fondo_verde.png') no-repeat center center fixed;
    background-size: cover;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
    margin: 0;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  }

  .card {
    background: rgba(255, 255, 255, 0.95);
    border-radius: 20px;
    padding: 3rem 2.5rem;
    max-width: 430px;
    width: 90%;
    box-shadow: 0 10px 30px rgba(0,0,0,0.3);
    text-align: center;
    animation: fadeInUp 0.9s ease;
  }

  .card img {
    width: 120px;
    margin-bottom: 20px;
    animation: bounceIn 1.2s;
  }

  h2 {
    font-weight: bold;
    margin-bottom: 1.5rem;
    color: #333;
  }

  label {
    font-weight: 600;
    margin-bottom: 0.5rem;
    display: block;
    text-align: left;
  }

  input[type="email"] {
    width: 100%;
    padding: 12px 15px;
    border: 1px solid #ccc;
    border-radius: 10px;
    margin-bottom: 1.5rem;
    font-size: 15px;
    transition: 0.3s;
  }

  input[type="email"]:focus {
    border-color: #007bff;
    box-shadow: 0 0 6px rgba(0,123,255,0.5);
    outline: none;
  }

  button {
    width: 100%;
    padding: 12px;
    border: none;
    background: linear-gradient(135deg, #007bff, #0056b3);
    color: #fff;
    font-size: 16px;
    font-weight: bold;
    border-radius: 10px;
    cursor: pointer;
    transition: transform 0.2s, box-shadow 0.3s;
  }

  button:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 15px rgba(0,0,0,0.2);
  }

  a {
    display: inline-block;
    margin-top: 1.5rem;
    text-decoration: none;
    color: #007bff;
    font-size: 14px;
    transition: color 0.2s;
  }

  a:hover {
    color: #0056b3;
  }
</style>

<div class="card animate__animated animate__fadeInUp">
  <img src="/images/contraseña.png" alt="Recuperar contraseña" class="animate__animated animate__bounceIn">
  <h2>Recuperar Contraseña</h2>

  <form action="/forgot-password" method="POST">
    <label for="email">Correo Electrónico</label>
    <input type="email" id="email" name="email" placeholder="tu_correo@ejemplo.com" required>
    <button type="submit">Enviar enlace de recuperación</button>
  </form>

  <a href="/login">⬅ Volver al login</a>
</div>
